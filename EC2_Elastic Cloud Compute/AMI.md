- Includes information on operating system and relevant configuration for resources and otherwise.
- Every AMI has an AMI IDs which are dynamically updated on updates or reinstalls?
- The cloudformation interpreter dynamically looks for an AMI ID attached to an EC2 specification

### What are AMI Mapprings?
